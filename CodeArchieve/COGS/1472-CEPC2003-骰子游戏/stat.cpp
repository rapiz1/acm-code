#include <cstdiO>
const int L = 7, N = L*L;
int nxt[N][L][2];
inline int zip(int i, int j) {return i*L + j;}
inline void unzip(int x, int& i, int& j) {j = x%L, i = x/L;}
int now[] = {0, 1, 2, 6, 5, :q
}
int main() {
}
