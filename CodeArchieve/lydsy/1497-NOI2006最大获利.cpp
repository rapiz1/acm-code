#include <cstdio>
#include <algorithm>
const int V = 5010, E = (5e4 +  V)*2;
int n, m, hed[V], nxt[E], sz, ans;
struct Edge{int u, v, c, f;}st[E];
void _add(int u, int v, int c) {
	st[++zs]
}
int main() {
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n; i++) {
		int x; 
		scanf("%d", &x);
		add(i, S, x);
	}
	for (int i = 1; i <= m; i++) {
		scanf("	
	}
}
