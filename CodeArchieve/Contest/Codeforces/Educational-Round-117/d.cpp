#include <iostream>
#include <algorithm>
#include <cstring>
#include <cstdio>
typedef long long ll;
using namespace std;
const int N = 1e5 + 10, V = N << 1, E = ;
int a[N][3];
ll d[N][3];
int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i++) for (int j = 1; j <= 3; j++) scnaf("%d", &a[i][j]);
	memset(f, , sizeof(-1));
}
